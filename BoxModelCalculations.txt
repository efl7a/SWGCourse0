Total height = margin top + border top + padding top + content + padding bottom + border bottom + margin bottom 
Total height =20+1+10+150+10+1+20 = 212px
Total width = margin left + border left + padding left + content + padding right + border right + margin right
Total width =20+1+10+400+10+1+20=462px
Browser calculated height=border top + padding top + content + padding bottom + border bottom 
Browser calculated height= 1+10+150+10+1 = 172px
Browser calculated width= border left + padding left + content + padding right + border right 
Browser calculated width= 1+10+400+10+1=422px